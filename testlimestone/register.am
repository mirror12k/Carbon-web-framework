<!doctype html>
<html>
<body>
<?perl
	if ($runtime->request->method eq 'GET') {
?><form method="POST">
<input type="text" name="username" />
<input type="password" name="password" />
<button>submit</button>
</form><?perl
	} elsif ($runtime->request->method eq 'POST') {
		my $args = $runtime->post_form;
		my ($username, $password) = @$args{'username', 'password'};
		use Data::Dumper;
		say Dumper $limestone->query(type => 'init', collection => 'users');
		my $res = $limestone->query(type => 'append', collection => 'users', data => [{ username => $username, password => $password }]);
		say "got response: ", Dumper $res;
		if ($res->is_success) {
			echo "success";
		} else {
			echo "failure";
		}
	}
?>
</body>
</html>
